version: '3.3'

services:
  mongodb:
    image: mongo:latest
    container_name: local-mongodb
    environment:
      MONGO_INITDB_DATABASE: PDAS
    ports:
      - 27017:27017
    volumes:
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
      - ./mongo-volumn:/data/db
